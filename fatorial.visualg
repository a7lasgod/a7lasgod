Algoritmo "fatorial"

Var
 c,num,f: inteiro


Inicio
  escreva("digite um numero:")
  leia(num)
  c <- num
  f <- 1
  repita
  f <- f * c
  c <- c - 1
  ate (c <1)
  escreval("o valor do fatorial de: ", num, " é ", f)


Fimalgoritmo
